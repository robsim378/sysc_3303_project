/*
package test;

import org.junit.Test;

import sysc_3303_project.Direction;
import sysc_3303_project.Elevator;

import static org.junit.Assert.assertEquals;

public class ElevatorTest {
    @Test
    public void testMoveElevator() {
        Elevator elevator = new Elevator(null, 1);
        elevator.moveElevator(5);
        assertEquals(5, elevator.elevatorFloor);
    }

    @Test
    public void testDirection() {
        Elevator elevator = new Elevator(null, 1);
        elevator.direction = Direction.UP;
        assertEquals(Direction.UP, elevator.direction);
    }

    @Test
    public void testElevatorID() {
        Elevator elevator = new Elevator(null, 1);
        assertEquals(1, elevator.elevatorID);
    }
}

 */
